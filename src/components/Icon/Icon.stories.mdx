import { Meta, Story } from "@storybook/addon-docs/blocks";

import Icon from "./Icon";

<Meta title="Icon" component={Icon} />

# Icon

The `Icon` component renders an SVG icon.
This component is also used on the `VisuallyHidden` component here to show how to add accessible text to icons.

<Story name="Default">
  <>
    <Icon id="search" />
  </>
</Story>
